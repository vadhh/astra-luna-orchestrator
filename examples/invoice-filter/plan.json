{
  "schema_version": 1,
  "project": "Synthetic invoice status filter example",
  "spec": "spec.md",
  "max_luna_workers": 1,
  "phases": [
    {"id": "P1", "goal": "A pure status filter with order preservation and input validation", "depends_on": [],
     "integration_checks": [{"command": "python3 -m unittest discover -s tests", "expected": "All repository tests pass in the hypothetical application"}]}
  ],
  "tasks": [
    {"id": "T1", "phase": "P1", "executor": "luna", "risk": "normal", "state": "planned", "depends_on": [],
     "brief": "tasks/T1.md", "allowed_paths": ["src/invoices/filtering.py", "tests/test_invoice_filtering.py"],
     "acceptance": ["Matches the chosen status while preserving order", "Invalid status raises ValueError", "Input collection is not mutated"],
     "checks": [{"command": "python3 -m unittest discover -s tests -p 'test_invoice_filtering.py'", "expected": "Filter behavior tests pass in the hypothetical application"}]}
  ]
}
