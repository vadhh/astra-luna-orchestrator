interface:
  display_name: "Astra + Luna Orchestrator"
  short_description: "Thin Astra orchestration; Luna builds and verifies."
  default_prompt: "Use $astra-luna-orchestrator in its thin-root default: Astra defines the contract and accepts once; one installed Luna worker owns implementation and verification."
policy:
  allow_implicit_invocation: true
